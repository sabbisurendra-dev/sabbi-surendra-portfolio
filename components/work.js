const metricBlock=m=>`<div class="metrics">${m.map(([v,l])=>`<div><strong>${v}</strong><span>${l}</span></div>`).join('')}</div>`;
function visual(p){if(p.type==='kuhoo')return `<div class="system">${['Student App','Application','Decisioning','LOS','Lender'].map((x,i)=>(i?'<i>→</i>':'')+`<div>${x}</div>`).join('')}</div>${metricBlock(p.metrics)}`;if(p.type==='workindia')return `<div class="funnel"><div><span>Existing users</span><b>→</b></div><div><span>Payment cohort</span><b>→</b></div><div><span>Revenue</span><b>→</b></div></div>${metricBlock(p.metrics)}`;if(p.type==='ola')return `<div class="scale-visual"><div class="big-number">30L<span> riders</span></div><div class="scale-lines"><span></span><span></span><span></span><span></span></div><div class="big-number">₹8.3L<span>/day saved</span></div></div>`;return `<div class="amazon-grid"><div><p class="project-copy">${p.copy}</p><div class="ai-result"><strong>95%</strong><span>prediction accuracy / precision</span></div></div><div class="review-card"><span>Customer Review</span><p>“Great battery life and a very comfortable fit...”</p><div class="keywords"><b>battery life</b><b>comfortable</b><b>fit</b></div><small>✓ Select to learn more</small></div></div>`}
export function renderWork(projects){return `<section class="section wrap" id="work"><div class="section-head"><p class="eyebrow">01 · Selected work</p><h2>Products built for <span>real outcomes.</span></h2><p>From a lending ecosystem built from scratch to growth systems, marketplace scale and applied AI.</p></div>${projects.map(p=>`<article class="project ${p.type}"><div class="project-top"><div><p class="tag">${p.tag}</p><h3>${p.name}</h3><h4>${p.title}</h4></div><span class="index">${p.id}</span></div>${p.type==='amazon'?'':`<p class="project-copy">${p.copy}</p>`}${visual(p)}</article>`).join('')}</section>`}