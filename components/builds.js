export function renderBuilds(data){return `<section class="section ai-section" id="ai"><div class="wrap"><div class="section-head"><p class="eyebrow">02 · AI & hands-on building</p><h2>I don’t just manage products.<br><span>I build them.</span></h2><p>Hands-on with AI tools, local models, prompt engineering and rapid prototyping — enough to turn an idea into something tangible, testable and shippable.</p></div><div class="build-grid">${data.builds.map(b=>`<div class="build-card ${b.type}"><div class="build-label">${b.label}</div><h3>${b.name}</h3><p>${b.copy}</p>${b.type==='vireo'?'<div class="terminal"><span>› model</span><b>local / Ollama</b><span>› workflow</span><b>agent + tools</b><span>› build</span><b>rapid prototype</b></div>':'<div class="game-art"><span>PRIMAL</span><i>●</i></div>'}<a href="${b.link}" target="_blank" rel="noreferrer">${b.linkText}</a></div>`).join('')}<div class="toolkit"><p class="tag">AI toolkit</p><div class="tool-pills">${data.tools.map(t=>`<span>${t}</span>`).join('')}</div></div></div></div></section>`}